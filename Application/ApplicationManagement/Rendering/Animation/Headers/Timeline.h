#pragma once

namespace Application {
namespace Rendering {
  // holds the information about a SPECIFIC animation (i.e. a list to all related keyframes) and the total duration of the animation
  // should also hold an identifier (hash of some sort?)
  struct Timeline
  {
  };
}// namespace Rendering
}// namespace Application