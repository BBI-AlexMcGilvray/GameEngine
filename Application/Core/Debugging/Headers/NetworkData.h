#pragma once

namespace Core
{
	namespace Networking
	{
		class NetworkData
		{
			// byte array that is comprised of all data

			// add data
				// calculates data size and stubs in the key (count to next block) and the data
		};
	}
}
