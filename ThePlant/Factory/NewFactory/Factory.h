#pragma once

/*
* For now, this will just be in charge of handling and updating assets, with the pipeline creating the UI for it.
* In the futuer, this should instead also hold default 'editor assets' (ui assets) that the pipeline/product will default open when build with the editor config
*/

// holds the loaded assets and is in charge of updating/saving changed assets
namespace Factory
{
    class FactoryManager
    {

    
    };
}